import 'package:flutter/material.dart';

class TreeGrowthAnimation extends StatelessWidget {
  const TreeGrowthAnimation({super.key});

  @override
  Widget build(BuildContext context) {
    // Replace with actual animation widget or code
    return const Center(
      child: SizedBox(
        width: 200,
        height: 200,
        child: Placeholder(), // Replace with your animation
      ),
    );
  }
}
